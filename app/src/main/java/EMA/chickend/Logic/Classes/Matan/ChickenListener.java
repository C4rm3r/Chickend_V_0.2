package EMA.chickend.Logic.Classes.Matan;

import EMA.chickend.Logic.Classes.Chicken;

// Need to replace with IBlowble !
public interface ChickenListener {
    void killChicken(Chicken chicken, boolean userTouch);
}
